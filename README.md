# 쥬라기 독서모임 📚

매달 한 권의 책을 함께 읽는 독서모임 아카이브.

## 배포 안내 (이미 작성된 별도 가이드 참고)

기술 스택: React + Vite + Tailwind + Supabase

## 환경변수

`.env` 파일을 만들고 다음 두 값을 입력하세요:

```
VITE_SUPABASE_URL=...
VITE_SUPABASE_ANON_KEY=...
```

(Vercel에 배포할 때는 Settings → Environment Variables에서 같은 두 값을 등록하세요)
